{
  "enable": {
    "type": "bool",
    "label": "启用插件",
    "default": true,
    "description": "开启或关闭 JMComic 漫画下载功能"
  },
  "download_base_dir": {
    "type": "string",
    "label": "下载根目录",
    "default": "data/jm_downloads",
    "description": "漫画下载和 PDF 存储的完整目录（如 C://Downloads），没有则自动创建，默认在 data/jm_downloads。支持中文路径。"
  },
  "pdf_quality": {
    "type": "int",
    "label": "PDF 图片质量",
    "default": 95,
    "description": "PDF 中图片的 JPEG 压缩质量 (1-100)，越高越清晰但文件越大"
  },
  "progress_updates": {
    "type": "bool",
    "label": "进度通知",
    "default": true,
    "description": "下载过程中是否在群里发送进度消息"
  },
  "domain_list": {
    "type": "string",
    "label": "自定义域名 (解决报错必填)",
    "default": "18comic.vip, 18comic.org",
    "description": "解决「请求重试全部失败」问题。如果默认域名失效，请填入最新的禁漫天堂有效域名（多个用英文逗号分隔）。"
  },
  "proxy": {
    "type": "string",
    "label": "代理服务器 (解决连不上)",
    "default": "",
    "description": "如果换了域名依然连不上，说明您的网络被墙，请配置代理（例如 http://127.0.0.1:7890 ）。留空则直连。"
  }
}
